apiVersion: v1
kind: Service
metadata:
  name: mortgage-loan-injection-go
  namespace: default
  labels:
    app: mortgage-loan-injection-go
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8085
      protocol: TCP
      name: http
  selector:
    app: mortgage-loan-injection-go
